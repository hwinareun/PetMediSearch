import styled from 'styled-components';

function Loading() {
  return (
    <LoadingStyle>
      <h1>Loading</h1>
    </LoadingStyle>
  );
}

const LoadingStyle = styled.div``;

export default Loading;
